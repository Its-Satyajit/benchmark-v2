{
  "name": "benchmark-v2",
  "version": "1.0.0",
  "description": "Multi-runtime and multi-stack application stress benchmark suite",
  "type": "module",
  "scripts": {
    "test": "nub --test test/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "benchmark": "./target/release/benchmark-runner data/92139349.json",
    "benchmark:all": "./target/release/benchmark-runner --all --iterations 10 data/92139349.json"
  },
  "dependencies": {
    "elysia": "^1.4.29"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "esbuild": "^0.25.0",
    "typescript": "^7.0.2"
  }
}
